apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: order-platform
type: Opaque
stringData:
  # Replace with actual password before applying
  SPRING_DATASOURCE_PASSWORD: secret
